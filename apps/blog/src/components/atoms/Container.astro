---
import { cx } from '../../utils/cx'

interface Props {
	children: any
	class?: string
}

const { class: className, children } = Astro.props
---

<div class={cx('container px-8 py-5 lg:py-8 mx-auto xl:px-5 max-w-screen-lg', className)}>
	<slot />
</div>
