'use strict'

/**
 * content-update-interface service.
 */

import { factories } from '@strapi/strapi'

const service = factories.createCoreService(
  'api::content-update-interface.content-update-interface'
)

export default service
