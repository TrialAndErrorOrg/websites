---
import { cx } from '../../utils/cx'

interface Props {
	children: any
	className?: string
}

const { className: className, children } = Astro.props
---

<div className={cx('container px-8 py-5 lg:py-8 mx-auto xl:px-5 max-w-screen-lg', className)}>
	<slot />
</div>
